# Stage 1.1 roadmap — executed

1. **Done.** Freeze protocol, config, split rules, gates, and Stage-1 artifact hashes.
2. **Done.** Add a batched learned state with RMS-normalized learned queries, a shared
   gated recurrent core, learned event encoder/access strength, and learned
   symbol/binary heads while preserving the memory law.
3. **Done.** Add structural and behavioral no-history-bypass tests.
4. **Done.** Select `1e-3` on development data only; train B0/B1/B2/B3/B5/B6
   with frozen formal seeds and budgets.
5. **Done.** Execute Experiments A–I, ablations A1–A7, machine-readable analysis, and
   frozen adjudication.
6. **Not run by protocol.** Experiment J required positive intermediate authorization;
   G7/G10/G11 failed, so long-stream execution was not authorized.
7. **Adjudicated NO-GO.** `STAGE2_LANGUAGE_MODEL_AUTHORIZED = FALSE`. Preserve
   the current failure evidence and redesign only under a new frozen protocol.
